const util = require('./util.js')
with(util) {
  describe('web功能', () => {
    describe('功能1', () => {
      it('用例1', () => {
        assert.ok(
          true
        )
      })
      it('用例2', () => {
        assert.ok(
          true
        )
      })
    })
    describe('功能2', () => {
      it('用例1', () => {
        assert.ok(
          true
        )
      })
      it('用例2', () => {
        assert.ok(
          true
        )
      })
    })
  })
}
